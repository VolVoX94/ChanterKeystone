<p>
<div class="container">

	<ul class="nav nav-tabs">
		<li id="one" class="active">
			<a data-toggle="tab" href="#home">
				{{#if isGerman}}
					Newsletter anmelden
				{{else}}
					S'abonner à la newsletter
				{{/if}}
			</a>
		</li>
			<li id="two" class="">
				<a data-toggle="tab" href="#menu1">
					{{#if isGerman}}
						Newsletter abmelden
					{{else}}
						Se désabonner de la newsletter
					{{/if}}
				</a>
			</li>
	</ul>

	<br/>
	
	<div class="tab-content">
		<div id="home" class="tab-pane fade in active">
			<div id="home" class="tab-pane fade in active">	
			<form action="/subscribeNewsletter" method = "post" class="form-horizontal">
				<input type="hidden" class="form-control" name="id" value="0">

				<div class="form-group">
					<label class="control-label col-sm-2" for="email">Email:</label>
					<div class="col-sm-10">
						<input required type="email" name="email" class="form-control" placeholder="Enter email">
					</div>
				</div>
				
				<div class="form-group">
					<label class="control-label col-sm-2" for="email">
						{{#if isGerman}}
							Passwort:
						{{else}}
							mot de passe:
						{{/if}}
					</label>
					<div class="col-sm-10">
						<input required type="password" name="password" id="password" class="form-control" placeholder="Enter new password" onkeyup='check();'>
					</div>
				</div>

				<div class="form-group">
					<label class="control-label col-sm-2" for="email">
						{{#if isGerman}}
							Passwort bestätigen:
						{{else}}
							Confirmer le mot de passe:
						{{/if}}
					</label>
					<div class="col-sm-10">
						<input required type="password" id="confirm_password" class="form-control" placeholder="Confirm new password" onkeyup='check();'>
					</div>
				</div>

				<div class="col-lg-10 col-lg-offset-2">
					<span id='message'></span>
				</div>

				<div class="form-group">
					<div class="checkbox">
						<label class="control-label col-sm-2" for="langSubscriber" >
							{{#if isGerman}}
								Deutsch:
							{{else}}
								Allemand:
							{{/if}}

						</label>
						<div class="col-sm-10">
							<input name="langSubscriber" type="checkbox" checked>
						</div>
					</div>
				</div>

				<div class="form-group">
					<div class="col-sm-offset-2 col-sm-10">
						<button id="submit" type="submit" value="submit" class="btn btn-default">
							{{#if isGerman}}
								Anmelden
							{{else}}
								Registré
							{{/if}}
						</button>
					</div>
				</div>
			</form>
		</div>
		</div>
		<div id="menu1" class="tab-pane fade">
			<form action="/unScribeNewsletter" method = "post" class="form-horizontal">
				<input type="hidden" class="form-control" name="unscribe" value="0">

				<div class="form-group">
					<label class="control-label col-sm-2" for="email">Email:</label>
					<div class="col-sm-10">
						<input required type="email" name="email" class="form-control" placeholder="Enter email">
					</div>
				</div>

				<div class="form-group">
					<label class="control-label col-sm-2" for="email">
						{{#if isGerman}}
							Passwort:
						{{else}}
							mot de passe:
						{{/if}}
					</label>
					<div class="col-sm-10">
						<input required type="password" name="passwordNewsletter" id="passwordNewsletter" class="form-control" placeholder="Enter your password">
					</div>
				</div>

				<div class="col-lg-10 col-lg-offset-2">
					<span id='message'></span>
				</div>

				<div class="form-group">
					<div class="col-sm-offset-2 col-sm-10">
						<button id="submitUnsubscribe" type="submit" value="submitUnsubscribe" class="btn btn-default">
							{{#if isGerman}}
								Abmelden
							{{else}}
								désabonner newsletter
							{{/if}}
						</button>
					</div>
				</div>
			</form>
		</div>	
	</div>	
</div>
</p>

<script>

	var check = function() {
		if (document.getElementById('password').value == document.getElementById('confirm_password').value) {
			document.getElementById('message').style.color = 'green';
			document.getElementById('message').innerHTML = 'matching';
			document.getElementById('submit').disabled = false;
		} else {
			document.getElementById('message').style.color = 'red';
			document.getElementById('message').innerHTML = 'not matching';
			document.getElementById('submit').disabled = true;
		}
	}
</script>
