<div class="container">
	{{#if comitees}}
		{{#if isGerman}}
			<h2>Leitender Ausschuss</h2>
		{{else}}
			<h2>Comité directeur</h2>
		{{/if}}
		
		{{#each comitees}}
			{{#each director}}
					<button type="button" class="btn bg-transparent" data-toggle="modal" data-target="#d{{@index}}">
						<p>{{name.first}} {{name.last}} ({{comiteRole}})</p>
					</button>
					<div class="modal fade" id="d{{@index}}" tabindex="-1" role="dialog" aria-labelledby="Modal1" aria-hidden="true">
						<div class="modal-dialog modal-dialog-centered modal-lg">
							<div class="modal-content">
								<div class="modal-header">
									<h5 class="modal-title" id="exampleModalLabel">{{name.first}} {{name.last}} ({{comiteRole}}</h5>
									<button type="button" class="close" data-dismiss="modal" aria-label="Close">
										<span aria-hidden="true">&times;</span>
									</button>
								</div>
								<div class="modal-body">
									{{#if image}}
										<img src="{{cloudinaryUrl image	 height='400'}}">
									{{/if}}
									{{#if ../../isGerman}}
										<h4>Kontact</h4>
									{{else}}
										<h4>Contact</h4>
									{{/if}}
									<div class="row">
										<div class="col-lg-3">
											<p><strong>
												Email: 
											</strong>{{email}}</p>
										</div>
										<div class="col-lg-3">
											<p><strong>
												{{#if ../../isGerman}}
													Position:
												{{else}}
													Place:
												{{/if}} 
											</strong>{{place}}</p>
										</div>
										<div class="col-lg-3">
											{{#if ../../isGerman}}
												<strong>Telefon:</strong>
											{{else}}
												<strong>Téléphone:</strong>
											{{/if}} <p>
												
											{{businessPhone}}</p>
										</div>
										<div class="col-lg-3">
											<p><strong>
												{{#if ../../isGerman}}
													Faxgerät:
												{{else}}
													Fax:
												{{/if}} 
											</strong>{{fax}}</p>
										</div>
									</div>
								</div>
							</div>
						</div>
					</div>
			{{/each}}
			{{#if ../isGerman}}
				<h2>Kantonalvorstand</h2>
			{{else}}
				<h2>Présidents des groupements</h2>
			{{/if}}
			{{#each president}}
				<button type="button" class="btn bg-transparent" data-toggle="modal" data-target="#p{{@index}}">
					<p>{{name.first}} {{name.last}} ({{comiteRole}})</p>
				</button>
				<div class="modal fade" id="p{{@index}}" tabindex="-1" role="dialog" aria-labelledby="Modal1" aria-hidden="true">
					<div class="modal-dialog modal-dialog-centered modal-lg">
						<div class="modal-content">
							<div class="modal-header">
								<h5 class="modal-title" id="exampleModalLabel">{{name.first}} {{name.last}} ({{comiteRole}}</h5>
								<button type="button" class="close" data-dismiss="modal" aria-label="Close">
									<span aria-hidden="true">&times;</span>
								</button>
							</div>
							<div class="modal-body">
								{{#if image}}
									<img src="{{cloudinaryUrl image	 height='400'}}">
								{{/if}}
								{{#if ../../isGerman}}
									<h4>Kontact</h4>
								{{else}}
									<h4>Contact</h4>
								{{/if}}
								<div class="row">
									<div class="col-lg-3">
										<p><strong>
											Email:
										</strong>{{email}}</p>
									</div>
									<div class="col-lg-3">
										<p><strong>
											{{#if ../../isGerman}}
												Position:
											{{else}}
												Place:
											{{/if}}
										</strong>{{place}}</p>
									</div>
									<div class="col-lg-3">
										{{#if ../../isGerman}}
											<strong>Telefon:</strong>
										{{else}}
											<strong>Téléphone:</strong>
										{{/if}} <p>

										{{businessPhone}}</p>
									</div>
									<div class="col-lg-3">
										<p><strong>
											{{#if ../../isGerman}}
												Faxgerät:
											{{else}}
												Fax:
											{{/if}}
										</strong>{{fax}}</p>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			{{/each}}
			{{#if ../isGerman}}
				<h2>Co-Präsidenten der Musikkommission</h2>
			{{else}}
				<h2>Co-Présidents de la Commission de Musique</h2>
			{{/if}}
			{{#each coPresident}}
				<button type="button" class="btn bg-transparent" data-toggle="modal" data-target="#cP{{@index}}">
					<p>{{name.first}} {{name.last}} ({{comiteRole}})</p>
				</button>
				<div class="modal fade" id="cP{{@index}}" tabindex="-1" role="dialog" aria-labelledby="Modal1" aria-hidden="true">
					<div class="modal-dialog modal-dialog-centered modal-lg">
						<div class="modal-content">
							<div class="modal-header">
								<h5 class="modal-title" id="exampleModalLabel">{{name.first}} {{name.last}} ({{comiteRole}}</h5>
								<button type="button" class="close" data-dismiss="modal" aria-label="Close">
									<span aria-hidden="true">&times;</span>
								</button>
							</div>
							<div class="modal-body">
								{{#if image}}
									<img src="{{cloudinaryUrl image	 height='400'}}">
								{{/if}}
								{{#if ../../isGerman}}
									<h4>Kontact</h4>
								{{else}}
									<h4>Contact</h4>
								{{/if}}
								<div class="row">
									<div class="col-lg-3">
										<p><strong>
											Email:
										</strong>{{email}}</p>
									</div>
									<div class="col-lg-3">
										<p><strong>
											{{#if ../../isGerman}}
												Position:
											{{else}}
												Place:
											{{/if}}
										</strong>{{place}}</p>
									</div>
									<div class="col-lg-3">
										{{#if ../../isGerman}}
											<strong>Telefon:</strong>
										{{else}}
											<strong>Téléphone:</strong>
										{{/if}} <p>

										{{businessPhone}}</p>
									</div>
									<div class="col-lg-3">
										<p><strong>
											{{#if ../../isGerman}}
												Faxgerät:
											{{else}}
												Fax:
											{{/if}}
										</strong>{{fax}}</p>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			{{/each}}
			{{#if ../isGerman}}
				<h2>Fähnrich und AGACH</h2>
			{{else}}
				<h2>Porte-Drapeau et délégué AGACH</h2>
			{{/if}}
			{{#each porteDrapeau}}
				<button type="button" class="btn bg-transparent" data-toggle="modal" data-target="#pD{{@index}}">
					<p>{{name.first}} {{name.last}} ({{comiteRole}})</p>
				</button>
				<div class="modal fade" id="pD{{@index}}" tabindex="-1" role="dialog" aria-labelledby="Modal1" aria-hidden="true">
					<div class="modal-dialog modal-dialog-centered modal-lg">
						<div class="modal-content">
							<div class="modal-header">
								<h5 class="modal-title" id="exampleModalLabel">{{name.first}} {{name.last}} ({{comiteRole}}</h5>
								<button type="button" class="close" data-dismiss="modal" aria-label="Close">
									<span aria-hidden="true">&times;</span>
								</button>
							</div>
							<div class="modal-body">
								{{#if image}}
									<img src="{{cloudinaryUrl image	 height='400'}}">
								{{/if}}
								{{#if ../../isGerman}}
									<h4>Kontact</h4>
								{{else}}
									<h4>Contact</h4>
								{{/if}}
								<div class="row">
									<div class="col-lg-3">
										<p><strong>
											Email:
										</strong>{{email}}</p>
									</div>
									<div class="col-lg-3">
										<p><strong>
											{{#if ../../isGerman}}
												Position:
											{{else}}
												Place:
											{{/if}}
										</strong>{{place}}</p>
									</div>
									<div class="col-lg-3">
										{{#if ../../isGerman}}
											<strong>Telefon:</strong>
										{{else}}
											<strong>Téléphone:</strong>
										{{/if}} <p>

										{{businessPhone}}</p>
									</div>
									<div class="col-lg-3">
										<p><strong>
											{{#if ../../isGerman}}
												Faxgerät:
											{{else}}
												Fax:
											{{/if}}
										</strong>{{fax}}</p>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			{{/each}}
		{{/each}}
	{{/if}}
</div>
